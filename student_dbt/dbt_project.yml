name: 'student_performance'
version: '1.0.0'
config-version: 2

profile: 'student_performance'

model-paths: ["models"]
analysis-paths: ["analyses"]

models:
  student_performance:
    example:
      staging:
        +materialized: view
        +schema: staging
      dimensions:
        +materialized: table
        +schema: dimensions
      facts:
        +materialized: table
        +schema: facts
